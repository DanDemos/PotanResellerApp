need phone number to transfer money
add voice when sending message button
search bar for game list

noti needs to redirect to user request
request time needs to be localized.
needs to add close button to requested image

coin topup history needs to save which rate was used and show only for admin.
