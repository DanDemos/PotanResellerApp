
import { useMemo } from 'react';

export function useLoginRouter(navigation: any) {
  return useMemo(
    () => ({
      // Handle navigation here if needed, e.g., to a Register screen
    }),
    [],
  );
}
